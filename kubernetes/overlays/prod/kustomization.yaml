apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - namespace.yaml
  - ../../base
  - configmap.yaml
  - secret.yaml

patches:
  - path: service-nodeport-patch.yaml